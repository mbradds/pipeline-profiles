import {
  cerPalette,
  conversions,
  visibility,
  sortJsonAlpha,
  arrAvg,
} from "../modules/util";
import { KeyPointMap } from "../modules/dashboard";

export async function mainApportionment(apportionData, lang) {
  return undefined;
}
